function sentensify(str) {
  // Only change code below this line
let duplicate = str.concat([]);
return duplicate.split(/\W/).join(' ');

  // Only change code above this line
}
sentensify("May-the-force-be-with-you");
